@page "/addperson"
@page "/addperson/{id:int}"
@using BlazorApp2.Data
@using BlazorApp2.Interfaces;
@inject HttpClient Http;
@inject IPersonService PersonService
<h3>addPerson</h3>


<PageTitle>Create new Person</PageTitle>
    
<EditForm Model="@newPerson" OnSubmit="handleSubmit">
    Put name
    <InputText id="name" @bind-bind-Value="newPerson.name"></InputText>
    Put age
    <!--
    <InputSelect @bind-bind-Value="newPerson.age" class="form-select">
        for (int i =0;i<100;i++)
        {

        }

    </InputSelect>
    -->
    <button type="submit" class="btn btn-primary">@btnText</button>
</EditForm>

@code {
    [Parameter] 
    public int? Id { get; set; }

    string btnText = string.Empty;

    Person newPerson = new Person();

    protected override async Task OnInitializedAsync()
    {
        btnText = Id == null ? "Save New Hero" : "Update Hero";
    }

    public async Task OnParametersSetAsync()
    {
        
    }

    async Task handleSubmit()
    {

    }
}